spring:
  application:
    name: modularMonolith

  datasource:
    url: jdbc:mysql://localhost:3306/springmodulith
    username: root
    password: gevic
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Déplacez ce bloc au même niveau que "spring:" (aligné complètement à gauche)
app:
  security:
    jwt:
      secret: "votre_cle_secrete_tres_longue_et_securisee_de_32_caracteres"
      expiration-ms: 86400000
