spring:
  application:
    name: modularMonolith

  datasource:
    url: jdbc:mysql://localhost:3306/springmodulith?useSSL=false&serverTimezone=UTC
    username: root
    password: gevic
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
