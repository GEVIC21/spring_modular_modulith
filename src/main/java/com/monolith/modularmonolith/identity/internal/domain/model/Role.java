package com.monolith.modularmonolith.identity.internal.domain.model;

/**
 * Rôles applicatifs définissant les permissions dans le système.
 */
public enum Role {
    STUDENT,
    TEACHER,
    ADMIN,
    SUPER_ADMIN,
    PARENT
}